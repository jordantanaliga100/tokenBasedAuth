# ========================
# 📦 Application Settings
# ========================
PORT=5000
NODE_ENV=development

# ========================
# 🟡 MySQL Configuration
# ========================
# Connection URI format:
# mysql://<username>:<password>@<host>:<port>/<database>
MYSQL_CONN_STRING=mysql://<username>:<password>@<host>:<port>/<database>

# Object-based configuration
MYSQL_DB_HOST=<host>
MYSQL_DB_PORT=<port>
MYSQL_DB_USER=<username>
MYSQL_DB_PASS=<password>
MYSQL_DB_NAME=<database>

# ========================
# 🔵 PostgreSQL Configuration
# ========================
# Connection URI format:
# postgresql://<username>:<password>@<host>:<port>/<database>
POSTGRES_CONN_STRING=postgresql://<username>:<password>@<host>:<port>/<database>

# Official PostgreSQL environment variables
PGHOST=<host>
PGPORT=<port>
PGUSER=<username>
PGPASSWORD=<password>
PGDATABASE=<database>

# Object-based configuration (for ORMs)
POSTGRES_DB_HOST=<host>
POSTGRES_DB_PORT=<port>
POSTGRES_DB_USER=<username>
POSTGRES_DB_PASS=<password>
POSTGRES_DB_NAME=<database>

# ========================
# 🟢 MongoDB Configuration
# ========================
# Connection URI format:
# mongodb://<username>:<password>@<host>:<port>/<database>?authSource=admin
MONGODB_CONN_STRING=mongodb://<username>:<password>@<host>:<port>/<database>?authSource=admin

# Object-based configuration (if needed)
MONGO_DB_HOST=<host>
MONGO_DB_PORT=<port>
MONGO_DB_USER=<username>
MONGO_DB_PASS=<password>
MONGO_DB_NAME=<database>

# ========================
# 🔒 Secrets and Security
# ========================
SESSION_COOKIE_NAME=session_id
SESSION_EXPIRES_IN_MINUTES=30
JWT_SECRET=<your-jwt-secret>
API_KEY=<your-api-key>


